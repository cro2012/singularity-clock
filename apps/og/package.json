{
  "name": "@sc/og",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Генерация OG-картинок. Чистая функция от параметров URL, без состояния.",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@sc/core": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "wrangler": "^3.100.0"
  }
}
