// Временная проба: минимальная функция второго поколения без единого импорта.
// Нужна, чтобы отделить поломку og.mts от поломки конфигурации проекта.
export default async (): Promise<Response> => new Response('pong');
